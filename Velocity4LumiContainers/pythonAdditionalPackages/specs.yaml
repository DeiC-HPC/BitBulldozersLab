versions:
  - spec:
      - 0.1
dependencies:
  - spec: python
    scope: build
  - spec: pythonFennol
    scope: build
  - spec: pythonJax
    scope: build
templates:
  - name: opensuse
    when: distro=opensuse