versions:
  - spec:
    - 4.2.3
    - 4.3.1
dependencies:
  - spec: libfabric
    scope: build
  - spec: fakeGpu
    scope: build
    when: system=lumig
templates:
  - name: opensuse
    when: distro=opensuse
