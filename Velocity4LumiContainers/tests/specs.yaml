versions:
  - spec: 1.0
variables:
  - name: OSU_VERSION
    value: 7.5
  - name: RCCL_DEBUG
    value: 0
dependencies:
  - spec: rocm
    scope: build
  - spec: fakeGpu
    scope: build
    when: system=lumig
  - spec: packages
    scope: build
  - spec: xcclPlugin
    scope: build
    when: system=lumig
  - spec: mpich
    scope: build
